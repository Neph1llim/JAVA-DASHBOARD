package main.themes;

//import com.formdev.flatlaf.FlatLaf;
import java.awt.*;
import javax.swing.*;

/*
public class CustomTheme implements Theme {

    
    public CustomTheme(Color bg, Color text, Font font, Color accent) {
        this.backgroundColor = bg;
        this.textColor = text;
        this.accentColor = accent;
        this.font = font;
    }

    @Override
    public void applyTheme() {
        //FlatLaf.setup();  // something wrong here
        UIManager.put("defaultFont", backgroundColor); // wrong key
        UIManager.put("defaultFont", textColor); // wrong key
        UIManager.put("defaultFont", accentColor); // wrong key
        UIManager.put("defaultFont", font);
    }

}
*/
